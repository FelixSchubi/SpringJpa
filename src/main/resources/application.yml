spring:
  datasource:
    url: jdbc:mysql://localhost:3306/User
    username: root
    password: 1860Unfall112!
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

    # spring.datasource.tomcat.max-active=1